# RefactorLoop Plan: Tooling-G3 Job + Lib

## Scope

| File | Path | Lines |
|---|---|---|
| Jenkinsfile | `jobs/dev/Tooling-G3/Jenkinsfile` | 86 |
| G3Client | `libs/JK-Lib/src/com/jklib/G3Client.groovy` | 97 |
| G3Models | `libs/JK-Lib/src/com/jklib/G3Models.groovy` | 204 |

## Parameters

- **Loop limit:** 4
- **Record folder:** `refact/Tooling-G3/` (created at init)
- **Review skill:** ImplementReview (per `docs/agents/ImplementReview.md`)

## Execution Process

### Init

1. Create record folder `refact/Tooling-G3/`
2. Capture baseline: copy all 3 files into `refact/Tooling-G3/baseline/` for before/after comparison
3. Record scope confirmation and starting line counts

### Loop 1 — Jenkinsfile functional pattern review

- Run ImplementReview on `Jenkinsfile` with spec (`jobs/dev/Tooling-G3/README.md`) and resource (`resources/G3-api/README.md`, `resources/job-as-function/README.md`)
- Focus: the repetitive switch-case pattern (6 cases, each following identical structure: parse input → call client → wrap output → writeReturnJson)
- Evaluate advice: is it sensible per code style rules (DRY > Readability > Line count)?
- Implement accepted changes
- Record before/after in `refact/Tooling-G3/loop-1.md`

### Loop 2 — G3Models data conversion review

- Run ImplementReview on `G3Models.groovy` with spec and code style rules
- Focus: repetitive fromMap/toMap/fromApiResult patterns across 12 inner classes
- Evaluate: can conversion logic be consolidated without violating "model class should hold data update/convert logic" rule?
- Implement accepted changes
- Record before/after in `refact/Tooling-G3/loop-2.md`

### Loop 3 — G3Client technical pattern review

- Run ImplementReview on `G3Client.groovy` with spec (`resources/G3-api/README.md`)
- Focus: request() method structure, error handling, method signatures
- Evaluate: alignment with code style rules (no defensive coding, validate only at public entrance)
- Implement accepted changes
- Record before/after in `refact/Tooling-G3/loop-3.md`

### Loop 4 — Cross-file coherence review

- Run ImplementReview on all 3 files together, checking:
  - Does the Jenkinsfile properly delegate to models/client?
  - Are model classes used consistently?
  - Any remaining DRY violations across the 3 files?
- Implement if sensible advice found
- Record in `refact/Tooling-G3/loop-4.md`

### Report and End

- Write final report to `refact/Tooling-G3/FINAL-REPORT.md`:
  - Summary of all changes across loops
  - Line count before/after per file
  - List of advice accepted vs rejected with reasons
  - Any remaining known issues or future improvement ideas

## Constraints

- Follow code style rules from `docs/JenkinsJob/AGENTS.md`:
  - Priority: DRY > Readability > Line count
  - Pipeline code should be simple, show main flow
  - Stage < 15 lines preferred
  - Model classes hold data conversion logic
  - Only validate user input, no defensive coding
- Follow spec template from `docs/spec/Jenkins-Job.md`
- Preserve all 6 JOB_FUNCTIONs and their behavior
- Preserve the job-as-function output contract (Return.json format)

## Validation

- After each loop: verify the Jenkinsfile still references all 6 JOB_FUNCTIONs correctly
- After each loop: verify G3Models still has all 12 Input/Output classes
- After each loop: verify G3Client still has all 6 API methods
- Final: run a mental walkthrough of each JOB_FUNCTION path to confirm no behavioral change

## Open Questions

- None — scope and parameters confirmed
